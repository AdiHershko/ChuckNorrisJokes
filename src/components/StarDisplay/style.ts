import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  starsContainer: {
    marginTop: 20,
    flexDirection: 'row',
  },
  star: {
    fontSize: 24,
    padding: 5,
  },
});

export default styles;
